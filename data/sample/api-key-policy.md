# API 密钥管理与签名校验

本文描述虚构产品 Northstar Cloud 的练习规则，不包含可用凭据。

API 密钥只应由服务器读取，不应写入前端代码、Git 仓库、错误日志或聊天消息。

计划内轮换每 90 天执行一次。轮换时先创建新密钥并更新服务端配置，验证成功后，在 24 小时内撤销旧密钥。怀疑泄露时应立即撤销，不等待计划窗口。

签名校验必须使用原始请求体，并验证请求时间戳；允许的时间偏差为 5 分钟。校验失败的请求不得进入业务处理。

本练习不提供真实签名算法和凭据配置，不能直接用于生产鉴权。
