# Webhook 投递与重试

本文描述虚构产品 Northstar Cloud 的练习规则，不对应任何真实服务。

Webhook 端点返回 HTTP 2xx 时，系统将本次投递标记为成功。

如果连接失败或端点返回非 2xx 状态，系统最多执行 3 次重试。三次等待间隔依次为 1 分钟、5 分钟和 30 分钟；这些间隔从前一次失败开始计算，不包含首次投递。

每次投递都包含稳定的 event_id。接收方应按 event_id 去重，避免重复执行业务操作。

生产环境修改端点地址前，应在测试环境完成验证。
