# 单因素检索实验记录

> 空白模板，没有执行就写“未运行”；fixture 结果不能填成真实模型质量。

## 实验问题

- 失败案例及类别：召回 / 排序 / 上下文 / 生成 / 引用 / 权限
- 假设与为什么只改这一项：
- 当前代码提交与工作区改动：
- 文档、题集 SHA256：
- 开发集、验证集、冻结测试集划分：
- 模型名、revision、embedding 维数、tokenizer：
- 机器、依赖快照、随机性、重复次数：

| 方案 | 唯一变化 | Recall@k | MRR | 人工支持率 | 无答案表现 | 延迟 p50/p95 | 实际费用/调用量 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 词法基线 | 无 | | | 不适用（无生成） | | | 无模型调用 | 未运行 |
| 向量基线 | 检索方式 | | | | | | | 未运行 |
| 混合 | RRF | | | | | | | 未运行 |
| 重排 | 候选后的 cross-encoder | | | | | | | 未运行 |
| 多查询 | 改写数量 | | | | | | | 未运行 |
| 父子 | 上下文扩展 | | | | | | | 未运行 |

样本很小时不夸大分位数或统计显著性。为各指标记录分母/样本数；报告按问题配对差异和失败类型。不要在看过冻结测试集后继续调参数还称它为“未见测试”。

## 一条变好、一条变差

分别记录输入、候选、原文、输出、预期和根因。安全/权限回归不能被均值改善抵消。

## 决策

- 采用 / 不采用 / 还需实验：
- 依据、成本、限制：
- 下一次只改变什么：
