# BM25与融合的离线演算：逐行精讲

[精讲总目录](index.md) · [对应源码](../../examples/hybrid_rankings.py)

本页是提前备好的阅读材料，不表示学习者已学过或已通过。行号对应当前完整源码；空行和注释也列出，但重点是执行语句的数据变化与边界。

## 先知道它解决什么问题

用明确token和固定第二榜演示BM25到RRF的数据结构。

### 输入、输出与调用关系

三篇词列表→词法排名，配一个手写榜单→融合分；再演示父级去重。

### 运行与风险边界

`python -m examples.hybrid_rankings`，仅标准库与项目代码。

vector_fixture不是模型输出；演示不可用于声称真实向量检索提升。

## 完整源码

<!-- source: examples/hybrid_rankings.py -->
```python
#: 这是排名机制实验，不是假装已跑完真实向量对照。
from evidencedesk.hybrid import bm25, rrf, expand_parents

#: 显式 token 列表保留词频，和阶段 1 的去重词集合形成对比。
def main():
    corpus = {"retry": ["webhook", "重试", "重试"], "keys": ["密钥", "轮换"], "ticket": ["故障", "工单"]}
    lexical = [doc_id for doc_id, score in bm25(["webhook", "重试"], corpus)]
    vector_fixture = ["keys", "retry"]
    print({"bm25": lexical, "fixture_vector": vector_fixture, "rrf": rrf([lexical, vector_fixture])})
    print(expand_parents(["child-a", "child-b"], {"child-a": "parent-1", "child-b": "parent-1"}))

if __name__ == "__main__":
    main()
```

## 逐行：语法、数据变化、理由与边界

同一条调用跨多行时，每行解释自己的参数或字段；同一物理行包含多个语句时，解释按执行次序展开。不用把闭合括号误读为另一次调用。

<a id="L1"></a>
### 第 1 行

```python
#: 这是排名机制实验，不是假装已跑完真实向量对照。
```

这是源码注释，不是执行语句。它提醒本段的设计意图：这是排名机制实验，不是假装已跑完真实向量对照。 实际是否满足要看随后的实现和测试，不能仅凭注释当成验证结论。

<a id="L2"></a>
### 第 2 行

```python
from evidencedesk.hybrid import bm25, rrf, expand_parents
```

**语法与数据变化：** 从 `evidencedesk.hybrid` 导入 `bm25, rrf, expand_parents`，把这些名称绑定到本模块；后面可直接使用，不必重复模块前缀。

**为什么与边界：** bm25按词频及文档频率评分；rrf按榜单位置融合；expand_parents将子ID映射到父ID并去重，三者都不是在线模型调用。 若模块不存在，应检查当前解释器及本课依赖，不要先改业务逻辑掩盖环境问题。

<a id="L3"></a>
### 第 3 行

空行分隔相邻逻辑，不创建变量、不读写文件，也不改变控制流。阅读时仍保留行号，方便与源文件定位一致。

<a id="L4"></a>
### 第 4 行

```python
#: 显式 token 列表保留词频，和阶段 1 的去重词集合形成对比。
```

这是源码注释，不是执行语句。它提醒本段的设计意图：显式 token 列表保留词频，和阶段 1 的去重词集合形成对比。 实际是否满足要看随后的实现和测试，不能仅凭注释当成验证结论。

<a id="L5"></a>
### 第 5 行

```python
def main():
```

**语法与数据变化：** 定义无参入口函数 main，本行创建函数对象，缩进体在调用时才执行；`-> None`（若写出）是返回类型提示。

**为什么与边界：** 将命令解析与业务调用集中到入口，便于测试单独导入其他函数；入口仍需正确处理下面的文件、参数和外部调用错误。

<a id="L6"></a>
### 第 6 行

```python
    corpus = {"retry": ["webhook", "重试", "重试"], "keys": ["密钥", "轮换"], "ticket": ["故障", "工单"]}
```

**语法与数据变化：** 手工构造ID到词列表字典，retry中的重试出现两次。

**为什么与边界：** 列表保留词频，区别于阶段1的集合去重；分词过程在例子外已人工指定。

<a id="L7"></a>
### 第 7 行

```python
    lexical = [doc_id for doc_id, score in bm25(["webhook", "重试"], corpus)]
```

**语法与数据变化：** 调用BM25并解包每个(ID,score)，只保存ID顺序。

**为什么与边界：** RRF需要的是排名而不是原始BM25数值，不能混用不同尺度分数。

<a id="L8"></a>
### 第 8 行

```python
    vector_fixture = ["keys", "retry"]
```

**语法与数据变化：** 显式命名vector_fixture，给出预置向量榜。

**为什么与边界：** 这是反事实演示输入，不是实际embedding计算，名称提醒不可误报实验结果。

<a id="L9"></a>
### 第 9 行

```python
    print({"bm25": lexical, "fixture_vector": vector_fixture, "rrf": rrf([lexical, vector_fixture])})
```

**语法与数据变化：** 打印词法榜、固定榜和融合结果。

**为什么与边界：** 融合能运行只能证明管道连接，不能单独证明相关性或泛化提升。

<a id="L10"></a>
### 第 10 行

```python
    print(expand_parents(["child-a", "child-b"], {"child-a": "parent-1", "child-b": "parent-1"}))
```

**语法与数据变化：** 两个子块都映射parent-1，打印去重后的父列表。

**为什么与边界：** 父子关系不是权限继承的自动授权，真实回填前仍需检查父级权限。

<a id="L11"></a>
### 第 11 行

空行分隔相邻逻辑，不创建变量、不读写文件，也不改变控制流。阅读时仍保留行号，方便与源文件定位一致。

<a id="L12"></a>
### 第 12 行

```python
if __name__ == "__main__":
```

**语法与数据变化：** `__name__` 在直接运行脚本/模块入口时是 `__main__`，导入时通常是模块名；条件决定是否进入下一行。

**为什么与边界：** 这样导入函数供测试使用时，不会自动执行此入口。注意其他顶层语句仍会在导入时执行，不能把整个文件视为绝对无副作用。

<a id="L13"></a>
### 第 13 行

```python
    main()
```

**语法与数据变化：** 调用上面定义的入口函数，开始执行其中的操作，不是仅取得函数对象。

**为什么与边界：** 本行通常位于入口保护条件下；可见输出、退出码与副作用由 main 的具体分支决定，异常若未被捕获会向上传播。

## 跟一遍数据与验证边界

retry含webhook和重复重试，词法可命中；两个child映射同parent时父列表只留一份。

## 只练一个关键点（不是新的学习验收记录）

1. 运行示例，分开抄下bm25与fixture_vector。
2. 只在副本调换fixture_vector顺序，观察RRF变化，保持词法输入不变。
3. **复盘：** 这个变化属于机制实验还是模型效果证据？

无需默写整份实现。涉及临时变异只在备份/副本里进行，完成后恢复；未来课程的联网、写库、上传和部署动作仍待相应阶段确认。

## 阅读完成不等于运行验收

本页逐行解释代码，不把源码中的 assert、测试 fixture 或演示输出冒充本轮实际运行结果。涉及网络、模型、数据库和部署的验证，仍按对应课程单独确认；报错时保留异常类型、输入与预期，不输出密钥。
