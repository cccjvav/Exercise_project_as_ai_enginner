# 按身份与版本隔离缓存键：逐行精讲

[精讲总目录](index.md) · [对应源码](../../examples/cache_scope.py)

本页是提前备好的阅读材料，不表示学习者已学过或已通过。行号对应当前完整源码；空行和注释也列出，但重点是执行语句的数据变化与边界。

## 先知道它解决什么问题

避免把一次授权范围内的回答缓存返回给别的用户或旧权限状态。

### 输入、输出与调用关系

身份、ACL版本、索引版本和问题组成规范JSON，再哈希成键；没有真实缓存服务器。

### 运行与风险边界

`python -m examples.cache_scope`，完全离线。

哈希不能匿名化可猜问题，也不是加密。调用方必须提供可信身份和有效版本，不能由用户伪造ACL版本。

## 完整源码

<!-- source: examples/cache_scope.py -->
```python
#: 缓存键可能成为旁路权限漏洞；演示键设计，不假装已经连接 Redis。
import hashlib
import json

#: 用户、授权版本、索引版本与问题共同决定键；JSON 数组避免简单拼接冲突。
def cache_key(tenant: str, user: str, acl_version: str, index_version: str, query: str) -> str:
    payload = json.dumps([tenant, user, acl_version, index_version, query], ensure_ascii=False)
    return "answer:" + hashlib.sha256(payload.encode("utf-8")).hexdigest()

#: 相同问题跨用户不可复用缓存，ACL 改变后也不能继续命中旧授权结果。
def main():
    a = cache_key("alpha", "alice", "acl-v1", "index-v1", "重试")
    b = cache_key("beta", "bob", "acl-v1", "index-v1", "重试")
    assert a != b
    assert a != cache_key("alpha", "alice", "acl-v2", "index-v1", "重试")
    print("PASS: 用户、租户和授权版本改变都会隔离缓存键")

if __name__ == "__main__":
    main()
```

## 逐行：语法、数据变化、理由与边界

同一条调用跨多行时，每行解释自己的参数或字段；同一物理行包含多个语句时，解释按执行次序展开。不用把闭合括号误读为另一次调用。

<a id="L1"></a>
### 第 1 行

```python
#: 缓存键可能成为旁路权限漏洞；演示键设计，不假装已经连接 Redis。
```

这是源码注释，不是执行语句。它提醒本段的设计意图：缓存键可能成为旁路权限漏洞；演示键设计，不假装已经连接 Redis。 实际是否满足要看随后的实现和测试，不能仅凭注释当成验证结论。

<a id="L2"></a>
### 第 2 行

```python
import hashlib
```

**语法与数据变化：** 导入 `hashlib` 模块；后续通过模块名访问接口。加载模块不等于已经完成下面的业务操作。

**为什么与边界：** 标准库摘要算法。sha256 接收字节，hexdigest 返回十六进制文本；哈希可作内容指纹，不是加密、权限校验或真实性认证。 若模块不存在，应检查当前解释器及本课依赖，不要先改业务逻辑掩盖环境问题。

<a id="L3"></a>
### 第 3 行

```python
import json
```

**语法与数据变化：** 导入 `json` 模块；后续通过模块名访问接口。加载模块不等于已经完成下面的业务操作。

**为什么与边界：** 标准库 JSON 编解码器。loads/读入负责解析，dumps 负责生成字符串；JSON 的 true/null 与 Python 的 True/None 对应，序列化不验证事实。 若模块不存在，应检查当前解释器及本课依赖，不要先改业务逻辑掩盖环境问题。

<a id="L4"></a>
### 第 4 行

空行分隔相邻逻辑，不创建变量、不读写文件，也不改变控制流。阅读时仍保留行号，方便与源文件定位一致。

<a id="L5"></a>
### 第 5 行

```python
#: 用户、授权版本、索引版本与问题共同决定键；JSON 数组避免简单拼接冲突。
```

这是源码注释，不是执行语句。它提醒本段的设计意图：用户、授权版本、索引版本与问题共同决定键；JSON 数组避免简单拼接冲突。 实际是否满足要看随后的实现和测试，不能仅凭注释当成验证结论。

<a id="L6"></a>
### 第 6 行

```python
def cache_key(tenant: str, user: str, acl_version: str, index_version: str, query: str) -> str:
```

**语法与数据变化：** 定义cache_key，要求五个维度的字符串。

**为什么与边界：** 签名不自动校验内容；少带用户或版本可能导致越权复用。

<a id="L7"></a>
### 第 7 行

```python
    payload = json.dumps([tenant, user, acl_version, index_version, query], ensure_ascii=False)
```

**语法与数据变化：** 把五项按固定顺序编码成JSON数组。

**为什么与边界：** 数组字段边界清晰，避免简单拼接冲突；问题是否规范化是另一个显式设计，本例保留原字符串。

<a id="L8"></a>
### 第 8 行

```python
    return "answer:" + hashlib.sha256(payload.encode("utf-8")).hexdigest()
```

**语法与数据变化：** UTF-8编码后取SHA-256，再加answer:命名空间前缀。

**为什么与边界：** 前缀区分键用途，不提供权限；哈希相同输入稳定，也不能证明缓存值安全。

<a id="L9"></a>
### 第 9 行

空行分隔相邻逻辑，不创建变量、不读写文件，也不改变控制流。阅读时仍保留行号，方便与源文件定位一致。

<a id="L10"></a>
### 第 10 行

```python
#: 相同问题跨用户不可复用缓存，ACL 改变后也不能继续命中旧授权结果。
```

这是源码注释，不是执行语句。它提醒本段的设计意图：相同问题跨用户不可复用缓存，ACL 改变后也不能继续命中旧授权结果。 实际是否满足要看随后的实现和测试，不能仅凭注释当成验证结论。

<a id="L11"></a>
### 第 11 行

```python
def main():
```

**语法与数据变化：** 定义无参入口函数 main，本行创建函数对象，缩进体在调用时才执行；`-> None`（若写出）是返回类型提示。

**为什么与边界：** 将命令解析与业务调用集中到入口，便于测试单独导入其他函数；入口仍需正确处理下面的文件、参数和外部调用错误。

<a id="L12"></a>
### 第 12 行

```python
    a = cache_key("alpha", "alice", "acl-v1", "index-v1", "重试")
```

**语法与数据变化：** 构造alpha/alice在两种v1状态下的键a。

**为什么与边界：** 这是示例输入，不是从真实鉴权系统读取。

<a id="L13"></a>
### 第 13 行

```python
    b = cache_key("beta", "bob", "acl-v1", "index-v1", "重试")
```

**语法与数据变化：** 同问题换成beta/bob，得到键b。

**为什么与边界：** 测试同时变了租户和用户，不能据此单独证明每个维度都被独立测试。

<a id="L14"></a>
### 第 14 行

```python
    assert a != b
```

**语法与数据变化：** 断言两个身份作用域的键不同。

**为什么与边界：** 防止最明显的跨身份缓存碰撞，但不模拟真实缓存命中和数据泄漏。

<a id="L15"></a>
### 第 15 行

```python
    assert a != cache_key("alpha", "alice", "acl-v2", "index-v1", "重试")
```

**语法与数据变化：** 仅变ACL版本再算键，要求与旧键不同。

**为什么与边界：** 权限变化后键失效是必要一步；旧数据清理、索引版本变化和缓存服务行为仍需另测。

<a id="L16"></a>
### 第 16 行

```python
    print("PASS: 用户、租户和授权版本改变都会隔离缓存键")
```

**语法与数据变化：** 输出键设计示例通过。

**为什么与边界：** 文本概括了设计意图，当前断言未分别覆盖所有字段变化，不能当全面生产隔离证明。

<a id="L17"></a>
### 第 17 行

空行分隔相邻逻辑，不创建变量、不读写文件，也不改变控制流。阅读时仍保留行号，方便与源文件定位一致。

<a id="L18"></a>
### 第 18 行

```python
if __name__ == "__main__":
```

**语法与数据变化：** `__name__` 在直接运行脚本/模块入口时是 `__main__`，导入时通常是模块名；条件决定是否进入下一行。

**为什么与边界：** 这样导入函数供测试使用时，不会自动执行此入口。注意其他顶层语句仍会在导入时执行，不能把整个文件视为绝对无副作用。

<a id="L19"></a>
### 第 19 行

```python
    main()
```

**语法与数据变化：** 调用上面定义的入口函数，开始执行其中的操作，不是仅取得函数对象。

**为什么与边界：** 本行通常位于入口保护条件下；可见输出、退出码与副作用由 main 的具体分支决定，异常若未被捕获会向上传播。

## 跟一遍数据与验证边界

同样“重试”问题，alpha/alice与beta/bob的键不同；ACL版本从v1变v2也不同。仍需保证缓存读取后的使用范围正确。

## 只练一个关键点（不是新的学习验收记录）

1. 运行示例；用临时交互调用固定其他参数、一次只改tenant/user/ACL/index中的一个。
2. 比较每次键是否变化，不连接Redis或记录真实用户问题。
3. **复盘：** 键隔离之外还缺少哪些读取授权/清理能力？

无需默写整份实现。涉及临时变异只在备份/副本里进行，完成后恢复；未来课程的联网、写库、上传和部署动作仍待相应阶段确认。

## 阅读完成不等于运行验收

本页逐行解释代码，不把源码中的 assert、测试 fixture 或演示输出冒充本轮实际运行结果。涉及网络、模型、数据库和部署的验证，仍按对应课程单独确认；报错时保留异常类型、输入与预期，不输出密钥。
