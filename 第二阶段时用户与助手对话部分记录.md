user: 进入阶段2吧。对了，你刚才提到明确的改进方向，后续我们还会提到这个吗，还是说就算完成了不谈了。
assistent: 会继续跟进，不会因为阶段 1 验收通过就把它们搁置。“通过”表示我们已经建立了可复现的基线、知道它哪里会失败，不表示这些问题已经解决。

我会把 q06、q09 的现状和后续复测位置明确记下来，并记录你已同意进入阶段 2。分块本身不保证修好它们；后面每次改进都要回到这些案例验证。


**会继续跟进，不会过关后就不谈了。** 阶段 1 通过的是学习目标，q06、q09 目前仍然是未解决的问题。

我已经把它们保存到**跨阶段回归跟踪表**，并把九道题整理成固定文件推送 GitHub，避免以后只记得成功案例。

| 问题 | 后续怎么跟进 |
|---|---|
| **q06：换说法就漏检** | 阶段 3 尝试真实语义检索；阶段 4 再按需要比较混合检索、查询改写等策略 |
| **q09：有关键词但无收费依据** | 阶段 3 检查证据不足时的回答与拒答；后续持续监测误召和无依据回答 |
| **两者共同** | 每次相关改动都复测，阶段 7 纳入持续回归评测 |

**不会承诺换一种技术就必然解决。** 必须有实际对照结果；没有改善，也要如实记录。检索仍返回背景、但回答能正确说明证据不足，也要分别报告，不能笼统说“全部修好了”。

---

## 现在进入 2A：把长文切成可定位的小段

阶段 2 先解决数据组织问题：文档变长、发生更新之后，怎样找到具体段落，并追踪它来自哪里。

先看最简单的**不重叠分块**。不需要新增依赖，下面直接调用项目已有函数：

```python
from evidencedesk.documents import Document
from evidencedesk.ingest import chunks


doc = Document(
    id="demo", title="分块示例", text="ABCDEFGHIJ", source="demo.md"
)
for chunk in chunks(doc, size=4, overlap=0):
    print(chunk["start"], chunk["end"], chunk["text"])
```

输出：

```text
0 4 ABCD
4 8 EFGH
8 10 IJ
```

### 逐行理解

- 两条 `import`：引入文档类和分块函数。
- `Document(...)`：手工构造一份示例文档，**不需要创建 `demo.md` 文件**。
- `size=4`：每块最多 **4 个字符**，不是 4 个 token。
- `overlap=0`：相邻块暂不重叠。
- `for chunk in ...`：每次取出一个块，打印位置和内容。

位置采用**左闭右开**区间：

```python
text[4:8]  # 包含位置 4，不包含位置 8，即 EFGH
```

最后剩下的 `IJ` 不足四个字符，也会保留。

分块还没有自动接入我们的检索器，也不会凭自身理解“再发”和“重试”的关系。

### 先预测，不用修改文件

同样是：

```text
ABCDEFGHIJ
```

如果改为 **`size=3`、`overlap=0`**，会得到几块？每块内容是什么？
user: 会得到四块内容为  
  
0 3 ABC  
  
3 6 DEF  
  
6 9 GHI  
  
9 10 J
assistent: 正确：一共四块，起止位置和内容都对，最后不足三个字符的 `J` 也会保留。

我会把这道题归档到 2A。接下来看看相邻块为什么有时需要保留一段重叠内容。


完全正确，四块的位置和内容都对：

```text
0 3 ABC
3 6 DEF
6 9 GHI
9 10 J
```

最后不足三个字符的 `J` 仍会保留。标准答案已归档到 **2A-Q1** 并推送 GitHub。

## 下一步：为什么需要重叠？

直接切块，可能把一句话的**条件和结论**分到不同块里。检索时只拿到其中一块，就可能缺少上下文。

因此，可以让相邻块共享一小段内容，这就是 `overlap`。它能降低边界处上下文断裂的风险，但不能保证语义一定完整。

关键公式是：

> **步长 = size − overlap**

- `size`：每块最多多少字符。
- **步长**：下一个块的起点前进多少字符。

### 完整示例：每块四个字符，重叠一个

```python
from evidencedesk.documents import Document
from evidencedesk.ingest import chunks


doc = Document(
    id="demo", title="分块示例", text="ABCDEFGHIJ", source="demo.md"
)
for chunk in chunks(doc, size=4, overlap=1):
    print(chunk["start"], chunk["end"], chunk["text"])
```

输出：

```text
0 4 ABCD
3 7 DEFG
6 10 GHIJ
```

这次只改变了分块参数：

```text
步长 = 4 − 1 = 3
```

所以起点依次是 `0、3、6`：

- 第一、二块共享 **D**。
- 第二、三块共享 **G**。
- 每块仍最多四个字符，并不是变成五个。

**当前实现还有一条停止规则：某块已经覆盖到全文末尾，就停止。** 因此不会再生成一个重复覆盖尾部的 `J` 小块。

### 先预测一下

仍然使用 `ABCDEFGHIJ`，改成：

```python
size=4
overlap=2
```

**步长是多少？会产生哪些块？** 像刚才一样写出起止位置和内容即可。
user: 步长为2，会输出下列内容

0 4 ABCD

2 6 CDEF

4 8 EFGH

6 10 GHIJ
assistent: 正确：步长是 `4 - 2 = 2`，四个块的位置和内容都对；最后一块已经覆盖到末尾，所以就停止了。

我会把这道题归档到 2A。接下来做一个小实验，观察重叠除了保留边界上下文，还会带来什么代价。


完全正确，**步长为 `4 − 2 = 2`**，四块的位置和内容都对。最后一块已经到达全文末尾，不会再生成一个重复的 `IJ` 小块。

标准答案已归档到 **2A-Q2** 并推送 GitHub。

## 关键实操：重叠会带来多少重复内容？

重叠可以保留边界附近的上下文，但原文长度不变，**所有块加起来的字符数却可能增加**。

我们做一个小对照：固定文本和 `size`，只改变 `overlap`。

### ① 新建完整示例

在仓库根目录新建 `scratch_overlap.py`，粘贴：

```python
from evidencedesk.documents import Document
from evidencedesk.ingest import chunks


doc = Document(
    id="demo", title="分块示例", text="ABCDEFGHIJ", source="demo.md"
)
print("overlap 块数 块内字符总数")
for overlap in (0, 1, 2):
    parts = chunks(doc, size=4, overlap=overlap)
    total_chars = sum(len(part["text"]) for part in parts)
    print(overlap, len(parts), total_chars)
```

### ② 理解新增部分

```python
for overlap in (0, 1, 2):
```

分别试三种重叠量，其他条件不变。

```python
parts = chunks(doc, size=4, overlap=overlap)
```

得到当前参数对应的块列表。

```python
total_chars = sum(len(part["text"]) for part in parts)
```

先计算每块的字符数，再求和。**共享字符在每个包含它的块中都会被计入。**

```python
print(overlap, len(parts), total_chars)
```

打印重叠量、块数和块内字符总数。

### ③ 运行

在原来的 Python 环境中执行：

```bash
python scratch_overlap.py
```

预期输出：

```text
overlap 块数 块内字符总数
0 3 10
1 3 12
2 4 16
```

原文始终只有 **10 个字符**。这里统计的是字符重复量，不是 token 数或实际费用。

**运行后告诉我：三行是否符合预期？为什么不能简单认为 `overlap` 越大越好？**
user: 三行符合预期，至于为什么不能简单认为overlap越大越好，我觉得这更多的是因为如果overlap太大，那么分块反而就没有意义了，并且消耗的空间也会增大
assistent: 实验结果确认了。你的方向是对的，尤其是“重复内容增多，会增加存储和处理负担”这一点。

不过，“分块没有意义了”可以说得更准确：较大的合法重叠不会取消分块，而是让相邻块越来越相似，增加的重复量未必能换来相应的检索收益。我会把原回答和这点补充一起归档。


你的理解方向正确，尤其是**重复内容增多，可能增加存储和处理负担**这一点。

不过，“分块没有意义了”稍微绝对了一些：它仍能限制每块长度，只是相邻块越来越相似，**额外重复量未必能换来相应的检索收益**。

这次实际测到的是字符重复量，不是磁盘空间或模型费用。实操和解释已归档到 **2A-Q3** 并推送 GitHub。

## 下一步：每个块来自哪里？

分块后不能只保存正文，否则拿到一段文字时，很难可靠回答：

- 它属于哪份文档？
- 来源是什么？
- 它在原文的哪个位置？

项目已经为每个块保存了这些信息：

| 字段 | 含义 |
|---|---|
| `parent_id` | 所属文档的 ID |
| `source` | 来源标识 |
| `start`、`end` | 在该版本正文中的字符范围 |
| `text` | 这一块的内容 |

### 完整小示例

先阅读即可，不必新建文件：

```python
from evidencedesk.documents import Document
from evidencedesk.ingest import chunks


doc = Document(
    id="demo", title="分块示例", text="ABCDEFGHIJ", source="demo.md"
)
part = chunks(doc, size=4, overlap=2)[1]

print(part["parent_id"], part["source"])
print(part["start"], part["end"], part["text"])
assert doc.text[part["start"]:part["end"]] == part["text"]
```

输出：

```text
demo demo.md
2 6 CDEF
```

### 逐行理解新的部分

```python
part = chunks(doc, size=4, overlap=2)[1]
```

取**第二块**。Python 列表索引从 `0` 开始，所以 `[1]` 是第二项。

```python
print(part["parent_id"], part["source"])
```

显示它属于文档 `demo`，来源字段是 `demo.md`。这里仍是手工对象，没有实际读取这个文件。

```python
assert doc.text[part["start"]:part["end"]] == part["text"]
```

根据保存的位置重新切出原文，验证内容确实一致：

```python
doc.text[2:6] == "CDEF"
```

注意：这些偏移针对 **`doc.text` 正文字符串**，不是整个 Markdown 文件的字节位置。

### 一个小检查

同一份 `doc` 拆出的四个块，**它们的 `parent_id` 应该相同还是不同？为什么？**
user: 他们的id相同，因为来源相同。  
assistent: 如果你指的是 `parent_id`，判断是对的：它们属于同一个 `Document`，所以都沿用同一个 `doc.id`。不过要和块自身的 `id` 区分，后者在这个例子中各不相同。

关于项目内的逐行讲解，我先核对一下实际覆盖范围。需要区分“教材中已经写好讲解”“我们在对话中已经学过”和“项目里所有代码都有讲解”，不能把它们混为一谈。


先确认你的答案：**如果指的是 `parent_id`，它们确实相同，因为属于同一个 `Document`。**

但要区分：

- `parent_id`：所属文档的 ID，这四块相同。
- 块自身的 `id`：区分具体块，这四块不同。
- `source`：来源标识，不能仅凭它相同就认定文档 ID 相同。

这道题和术语补充已归档到 **2A-Q4**。